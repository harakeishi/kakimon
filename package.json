{
  "name": "kakimon",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "pnpm --filter @kakimon/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "preview": "pnpm --filter @kakimon/web preview"
  },
  "engines": {
    "node": ">=20"
  }
}
